//Конечные роуты
const endpoints = {
    AUTH: {
        LOGIN: '/authenticate',
        PROFILE: '/GetRole',
        LOGOUT: '/logout',
        REFRESH: '/refreshToken',
    }
}

export default endpoints;